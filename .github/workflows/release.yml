name: "Release"

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    strategy:
      fail-fast: false
      matrix:
        platform: [macos-latest, ubuntu-22.04, windows-latest]

    runs-on: ${{ matrix.platform }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - uses: dtolnay/rust-toolchain@stable

      - name: install dependencies (ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev libayatana-appindicator3-dev librsvg2-dev flatpak-builder

      - name: install frontend dependencies
        run: npm install

      - name: build and release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: v${{ github.ref_name }}
          releaseName: "App Release ${{ github.ref_name }}"

      # 将 Flatpak 构建作为 Ubuntu 任务的一个额外步骤
      - name: build flatpak bundle (ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          npm run tauri build
          flatpak-builder build-dir com.cqhqp.hello-tauri.json
          flatpak-builder --bundle-version=0.1.1 --bundle-id=com.cqhqp.hello-tauri --bundle-version-git hello-tauri.flatpak build-dir

      - name: upload flatpak artifact (ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        uses: softprops/action-gh-release@v1
        with:
          files: hello-tauri.flatpak
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
